SELECT a.ID_ARTICLE , a.NOM_ARTICLE , a.VOLUME , SUM(v.QUANTITE) as Total FROM article a INNER JOIN ventes v ON a.ID_ARTICLE = v.ID_ARTICLE INNER JOIN type t ON a.ID_TYPE = t.ID_TYPE WHERE v.ANNEE = 2016 AND t.NOM_TYPE = "Trappiste" GROUP BY a.ID_ARTICLE ORDER BY Total DESC LIMIT 5;